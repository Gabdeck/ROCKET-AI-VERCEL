{
  "buildCommand": "npm run build",
  "outputDirectory": ".output",
  "framework": null,
  "functions": {
    "api/server.js": {
      "includeFiles": ".output/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/server" }
  ]
}
